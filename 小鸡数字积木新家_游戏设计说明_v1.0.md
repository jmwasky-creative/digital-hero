# 《小鸡数字积木新家》游戏设计说明

> 设计版本：V1.0
> 日期：2026-07-25
> 产品形态：HTML5 / PWA 儿童数学游戏
> 目标年龄：4.5～6 岁
> 需求基线：`chick_number_blocks_game_requirements_v1.0.md`
> 文档用途：交互原型、视觉设计、技术架构、规则引擎、数据设计、测试和多 Agent 开发的共同基线

---

## 1. 设计结论

《小鸡数字积木新家》不是选择答案后播放奖励动画的答题器。它的核心是：

> 儿童看见数量，通过点击或搬动积木让数量真实变化，再由小鸡把正确数量的积木转化为新家的一部分。

最小教学单位不是“一道题”，而是一次可观察、可操作、可验证的数量变化：

```text
数学关系
  ↓
积木增加、减少、组合、展开或掉落
  ↓
数字牌、空位、语音和动画同步变化
  ↓
儿童确认结果或在提示下修正
  ↓
小鸡完成一次真实建造
```

首版采用“离线优先客户端 + 共享确定性规则引擎 + Node.js 权威投影 + 可拔除 AI 阶段分析”的整体设计。

---

## 2. 第一性原则与不可破坏的不变量

### 2.1 数量状态是唯一事实源

任何任务都必须满足：

```text
数学状态中的数量
 = 已放置单位积木数 + 十积木数 × 10
 = 当前数量牌
 = 目标槽位和空位
 = 动画增加或掉落的积木数
 = 语音读数
 = 算式
 = 学习记录
```

DOM、Canvas、动画和语音不得各自维护数量。它们只能渲染统一领域状态。

### 2.2 数学与表现分离

- `MathProblem` 描述数量关系、约束和可接受答案。
- `PresentationPlan` 描述小鸡、颜色、建造部件、场景和语音键。
- `InteractionPlan` 描述点击、拖动、选择、比较或判断修正。
- 同一数学结构可以更换故事和交互，但答案与合法性不能变化。

### 2.3 操作错误不等于数学错误

拖偏、取消拖动、页面切换、动画中断、误触和设备卡顿不得记为数学错误。只有儿童在数量稳定后明确提交答案，才进入数学判定。

### 2.4 儿童永远可以继续

- 普通任务无失败倒计时。
- 答错不摧毁已经建好的新家，不扣除已有奖励。
- 第三层提示采用共同完成，仍让儿童执行最后一步。
- 反应慢不能直接导致降级或失败。

### 2.5 AI 不进入儿童实时路径

出题、判题、提示、奖励、课程门控和实时调难全部由确定性代码控制。AI 每完成 5 个小关最多调用一次，只丰富家长报告，失败不影响游戏、解锁和规则调难。

---

## 3. 首版范围

### 3.1 MVP 内容规模

- 1 套小鸡新家主题；
- 3 只基础小鸡；
- 6 个章节，每章 5 个小关，共 30 个小关；
- 每关 5 个任务，由规则引擎动态生成；
- 数字范围 1～20；
- 数数、匹配、比较、合并、拿走、掉落、补足、十积木、跨 10、判断修正；
- 点击和拖动两条等价交互路径；
- 规则动态难度、离线保存、阶段报告和家长设置；
- AI 兼容适配和规则降级。

### 3.2 延后内容

- 更多主题、服装和动物伙伴；
- 教师管理后台；
- 成就相册；
- 多设备档案合并；
- 开放式儿童 AI；
- 高精度物理搭建；
- 商城、付费、广告、排行榜和社交。

### 3.3 MVP 验证目标

首版首先验证四件事：

1. 儿童能否理解“现在有、目标是、还差多少”；
2. 数字积木展开成单位积木是否能帮助理解数量；
3. 掉落和补回是否能自然承载减法与缺数；
4. 新家成长能否持续强化学习行为，而不抢走对数量的注意力。

---

## 4. 世界观、角色与成长

### 4.1 核心故事

三只小鸡来到一片空地，要一起搭建新家。建造过程中会出现积木不够、风吹掉积木、伙伴送来积木、两边高度不同、十块小积木组合等问题。儿童通过数学操作帮助小鸡解决这些问题。

### 4.2 小鸡职责

- 搬运小鸡：把积木送到操作区；
- 建造小鸡：在数量正确后完成吸附和建造；
- 接球小鸡：接住掉落积木、提供提示和鼓励。

角色只承担故事、引导和反馈，不能遮挡数字、槽位和主要操作路径。

### 4.3 新家成长

成长顺序采用可配置清单：地板、墙、门、窗、左右塔、屋顶、玩具房、小桥、花园、数字塔、完整新家。

每次任务只完成一个明确的小变化；章节完成后解锁一个较大的部件或玩具。奖励必须在数学反馈之后出现：先确认“8 块刚刚好”，再展示屋顶建成。

---

## 5. 信息架构与核心屏幕

### 5.1 屏幕流程

```text
启动与恢复
  → 轻量建角
  → 三步首次引导
  → 新家总览/章节地图
  → 关卡任务
  → 单任务建造反馈
  → 关卡结算
  → 章节完成/阶段报告
  → 新家总览
```

家长区域独立于儿童游戏流，通过长按指定区域和成人操作门槛进入。

### 5.2 建角页

儿童只用图标选择：

- 羽毛颜色；
- 帽子；
- 背包；
- 表情；
- 预设昵称。

不提供自由文本，不要求真实姓名。

### 5.3 关卡主画面

横屏优先，固定为四层：

```text
┌────────────── 数量与进度栏 ──────────────┐
│ 第 2/5 任务   现在有 5   目标 8   还差 ? │
├────────────── 建造目标区 ────────────────┤
│ 屋顶/墙/桥 + 半透明合法槽位 + 新家预览    │
├────────────── 积木操作区 ────────────────┤
│ 单位积木、数字积木、两堆积木或数字选项     │
├────────────── 小鸡活动区 ────────────────┤
│ 搬运、建造、接球、语音重播与提示           │
└─────────────────────────────────────────┘
```

一次任务只突出一种主要动作，避免同时出现过多按钮、积木和角色动作。

### 5.4 竖屏布局

竖屏重排为“数量牌 → 建造区 → 操作区 → 小鸡栏”，不缩小触控目标。关键触摸区域不小于 `56 × 56 CSS px`，单位积木建议不小于 `64 × 64 CSS px`。

### 5.5 家长区域

- 游戏时长；
- 语音、音效和音乐；
- 减弱动画；
- 自动调难；
- 最大数字范围；
- 十积木展示；
- 阶段报告；
- 数据导出与删除。

到达时长只柔和提醒休息，不判失败、不清除当前任务。

---

## 6. 核心领域模型

### 6.1 数量模型

```ts
type QuantityModel = {
  tens: 0 | 1 | 2;
  ones: number;
  value: number;
};
```

必须满足：

- `value = tens × 10 + ones`；
- 规范状态下 `0 <= ones < 10`；
- C1～C4 使用单位积木表现；
- C5 起引入十积木；
- 十积木展开后必须恰好得到 10 个单位积木；
- 组合和展开前后总量守恒。

### 6.2 积木模型

```ts
type BlockKind = 'unit' | 'number' | 'ten' | 'ghost';

type Block = {
  id: string;
  kind: BlockKind;
  representedUnits: number;
  color: 'yellow' | 'blue' | 'red' | 'green';
  location: 'tray' | 'build-area' | 'fallen-area';
  visualState:
    | 'idle'
    | 'dragging'
    | 'snapping'
    | 'placed'
    | 'falling'
    | 'returning'
    | 'expanded'
    | 'locked';
  sourceId: string;
  slotId?: string;
};
```

规则：

- 单位积木代表 1；
- 数字积木 `[3]` 代表 3 个单位，展开后产生 3 个带相同来源标识的单位积木；
- 十积木代表 10，不得按“普通一块”参与点数；
- `ghost` 是透明目标空位，不计入当前数量；
- `currentValue` 必须从已放置积木派生，禁止单独修改。

### 6.3 任务规格

```ts
type TaskSpec = {
  schemaVersion: 1;
  rulesetVersion: string;
  curriculumVersion: string;
  taskId: string;
  seed: string;
  chapterId: number;
  levelId: string;
  slot: 1 | 2 | 3 | 4 | 5;
  skillCode: SkillCode;
  questionType: QuestionType;
  operands: Record<string, number>;
  expectedAnswer: unknown;
  maxValue: 5 | 10 | 15 | 20;
  allowCrossTen: boolean;
  allowedInteractions: InteractionType[];
  presentation: PresentationPlan;
  remediation: RemediationPlan;
};
```

题目、场景、交互、支架和错误修复分别配置，便于替换视觉而不改数学逻辑。

### 6.4 任务运行状态

```ts
type TaskRuntime = {
  taskId: string;
  phase: TaskPhase;
  blocks: Record<string, Block>;
  currentValue: number;
  targetValue?: number;
  hintLevel: 0 | 1 | 2 | 3;
  confirmedAttemptCount: number;
  interactionErrorCount: number;
  mathErrorCount: number;
  completionMode?: 'SELF' | 'GUIDED' | 'SYSTEM_ASSISTED';
};
```

刷新或动画中断后，界面从 `TaskRuntime` 恢复，不依赖动画播放到哪一帧。

---

## 7. 题型与交互

| 题型 | 数学关系 | 首选交互 | 低精度替代 |
|---|---|---|---|
| 数到目标 | 从当前量补到目标量 | 逐块拖入 | 点击积木再点目标 |
| 找数字 | 数字与数量匹配 | 点击/配对 | 大按钮选择 |
| 合并总数 | `a + b = total` | 合并两堆 | 选择数字积木 |
| 拿走后剩余 | `original - removed = remaining` | 拖走/点掉积木 | 选择剩余数 |
| 掉了多少 | `original - ? = remaining` | 找回掉落堆 | 选择掉落数 |
| 还差多少 | `current + ? = target` | 填充透明空位 | 选择数字积木 |
| 补回原数 | `remaining + ? = target` | 把积木放回 | 选择补回数 |
| 比较多少 | more / less / equal / difference | 选一边并对齐 | 大按钮选择 |
| 凑 10 | `a + b = 10` 多解 | 两色积木自由组合 | 选择一组合法搭法 |
| 十和几个一 | `10 + n` | 十积木 + 单位积木 | 数字选择 |
| 判断并修正 | 判断真假并替换错误值 | 两阶段操作 | 判断按钮 + 数字按钮 |

判断题只有完成“判断”和“修正”两个步骤才算独立正确。

---

## 8. 拖动、吸附与动画

### 8.1 拖动流程

1. `pointerdown`：积木进入 `dragging`；
2. `pointermove`：跟手移动，进入合法投放范围时高亮；
3. `pointerup/cancel`：
   - 接近合法槽位时自动吸附；
   - 数量上不合法时柔和回弹；
   - 拖偏或取消不记数学错误；
4. 吸附完成后提交领域事件，领域状态先更新，再播放数量动画；
5. 重复提交通过事件 ID 幂等处理。

吸附范围取目标宽高的约 40% 或最小 `48px` 半径，避免要求精确摆放。

### 8.2 增加动画

- 每块积木依次进入槽位；
- 数量牌同步执行 `5 → 6 → 7 → 8`；
- 每次变化读出当前数；
- 所有积木到位后再显示算式和建造反馈。

### 8.3 掉落动画

- 先在领域事件中确定准确掉落数量和积木 ID；
- 高亮即将掉落的积木；
- 逐块掉落并同步减少数量牌；
- 动画中断时根据领域事件恢复最终数量；
- 掉落不能造成已有建筑坍塌。

### 8.4 十积木动画

以 `9 + 3 = 12` 为例：

1. 展示 9 个单位；
2. 加入 3 个单位；
3. 框选其中 10 个；
4. 压缩为一个带十格纹理的 `[10]`；
5. 保留 2 个单位；
6. 语音：“1 个十和 2 个一，是 12。”

低动画模式用高亮和淡入替代路径动画，但数量状态不变。

### 8.5 DOM、Canvas 和音频边界

- DOM：数字牌、按钮、可交互积木代理、家长设置、焦点与无障碍信息；
- Canvas 2D：小鸡动作、背景、掉落路径、组合动画和装饰粒子；
- Pointer Events：拖动、点击和触摸容错；
- Web Audio / HTMLAudio：吸附、掉落、成功、提醒和背景音乐；
- Canvas 不承担唯一交互层或判题逻辑。

每个 Canvas 可交互积木都有对应 DOM 代理，支持键盘和“选择积木 → 放到发光位置”的替代流程。

---

## 9. 前端状态机

```text
LEVEL_IDLE
  → TASK_INTRO
  → TASK_READY
  → INTERACTING
  → SNAP_PREVIEW
  → APPLYING_QUANTITY_CHANGE
  → CHECKING
  ├─ CORRECT → BUILDING → REWARDING → NEXT_TASK / LEVEL_COMPLETE
  ├─ INCOMPLETE → TASK_READY
  ├─ WRONG → HINT_1 → TASK_READY
  ├─ WRONG_AGAIN → HINT_2 → TASK_READY
  └─ THIRD_ERROR → GUIDED_COMPLETE → BUILDING → REWARDING
```

系统附加状态：

- `PAUSED`：切后台、家长区域或休息提醒；
- `OFFLINE_PENDING_SYNC`：本地已完成、事件待同步；
- `ANIMATION_RECOVERY`：动画中断后重绘最终状态；
- `ACCESSIBLE_ACTION_MODE`：使用点击替代拖动。

只有 `INTERACTING` 和确认阶段允许数学提交；任何状态都可以重播当前语音。

---

## 10. 语音、音效和提示

### 10.1 音频管理

使用统一 `AudioManager`，分别管理：

- `voice`：任务语音和逐个读数；
- `effect`：点击、吸附、掉落、成功和柔和提醒音；
- `music`：低音量背景音乐。

任务语音以短句预录音为首选，`speechSynthesis` 作为降级。语音失败不阻塞游戏，所有关键含义都有数字、积木和图示。

### 10.2 播放规则

- 新任务稳定显示 500ms 后自动播报；
- 点击数字积木时立即读出该数字；
- 放入单位积木时读出变化后的当前数量；
- 正确使用明亮但短促的成功音；
- 错误使用柔和提醒音，不使用警报或失败音；
- 喇叭按钮可重复当前任务；
- 任务语音开始时背景音乐自动降低；
- 移动浏览器首次用户操作后初始化音频。

### 10.3 三层提示

| 条件 | 层级 | 表现 |
|---|---:|---|
| 首次明确错误或 6 秒无操作 | 1 | 目标轻光、相关积木逐个发光、语音“我们再数一次” |
| 第二次错误或 10 秒无操作 | 2 | 显示当前实物、目标实物、圆点和透明空位 |
| 第三次错误或 15 秒仍无操作 | 3 | 一次只高亮一个正确步骤，儿童点击后共同完成 |

儿童重新操作后取消后续无操作计时。共同完成计最终完成，但不计独立掌握。

---

## 11. 课程结构

需求中的完整技能图为 8 个阶段；MVP 将相邻阶段压缩为 6 章，以 30 个小关覆盖首版 P0 能力。

| MVP 章 | 场景 | 数字范围 | 核心能力 | 建造成果 |
|---|---|---:|---|---|
| C1 积木仓库 | 训练场 | 1～5 | 认数、点数、数量匹配、搬到目标 | 地板 |
| C2 积木变多了 | 仓库 | 1～10 | 合并、比较、总数 | 第一面墙、门 |
| C3 大风来了 | 户外 | 1～10 | 拿走、掉落、剩余、掉了多少 | 窗户和墙 |
| C4 双塔一样高 | 双塔 | 1～10 | 还差多少、差值、分解、凑 10 | 左右塔 |
| C5 十块变一块 | 数字塔 | 1～20 | 十积木、10 加几、十和几个一、不跨十 | 数字塔、大城门 |
| C6 风暴保卫战 | 完整新家 | 1～20 | 跨十加减、补回、混合、判断修正 | 屋顶与完整新家 |

扩展版可将 C5、C6 拆为原需求中的 C5～C8，获得更长课程节奏。

### 11.1 单关五任务结构

1. 已掌握内容暖场；
2. 当前学习内容；
3. 积木不够/缺数；
4. 掉落/拿走/剩余；
5. 多只小鸡合作建造。

每关 2～4 分钟，每 5 关形成一个报告检查点。

---

## 12. 规则出题引擎

### 12.1 生成输入

```ts
type GenerationInput = {
  runSeed: string;
  rulesetVersion: string;
  curriculumVersion: string;
  chapterConfig: ChapterConfig;
  difficulty: DifficultyVector;
  recentHistory: QuestionFingerprint[];
  weakSkills: SkillCode[];
  slot: 1 | 2 | 3 | 4 | 5;
};
```

服务器创建关卡时生成并持久化 `runSeed`。每题子种子为：

```text
hash(rulesetVersion, runSeed, chapterId, levelId, slot, remediationOrdinal)
```

题目代码只使用注入式确定性 PRNG，禁止调用 `Math.random()` 或当前时间。相同版本、种子和配置必须生成相同规范化题目。

### 12.2 选题比例

- 60% 当前学习内容；
- 20% 已掌握复习；
- 15% 近期薄弱；
- 5% 轻度挑战。

首次无历史时，薄弱位回流到普通复习。

### 12.3 防重复

- 最近 10 道不出现相同数学指纹；
- 最近 5 道避免交换题紧邻；
- 相同答案不连续出现 3 次；
- 相同交互不连续出现 3 次；
- 错题不在下一题原样重现；
- 错题在 2～4 道后以新场景或新支架复现；
- 候选不足时只允许放宽答案连续、交互连续和交换题限制；数学合法性永不放宽。

### 12.4 数学合法性

- 所有数在当前 `maxValue` 内；
- 加法结果不超范围；
- 减法非负；
- 未开放跨十时不得生成跨十结构；
- 十积木始终等于 10 个单位；
- 数字、实物、空位、动画、语音和答案完全一致；
- 选择题只有一个正确答案；
- 多解题显式保存完整合法答案集合；
- 干扰项唯一且在当前范围内。

---

## 13. 动态难度

### 13.1 难度向量

```ts
type DifficultyVector = {
  maxValue: 5 | 10 | 15 | 20;
  operationForm: 'count' | 'direct' | 'missing' | 'mixed';
  crossTen: boolean;
  concreteSupport: 'full' | 'partial' | 'none';
  showDots: boolean;
  showEmptySlots: boolean;
  optionCount: 2 | 3;
  distractorDistance: 'far' | 'near';
  interaction: 'tap' | 'choose' | 'drag';
  languageContext: 'minimal' | 'story';
};
```

难度按技能保存，不使用单一全局等级。

### 13.2 降低难度

触发条件：

- 连续 2 个首次数学错误；
- 同一能力最近 5 题错误率超过 50%；
- 最近 3 题持续使用二级或三级提示；
- 跨十持续失败；
- 长时间无操作或随机点击只触发操作支架，不直接降低数学掌握。

一次最多改变 1～2 个维度，优先增加实物、圆点和空位，减少选项，切换点击/逐块操作，再考虑缩小数字范围或暂停跨十。

### 13.3 提高难度

至少有 5 题样本，并满足大部分条件：

- 首次正确率不低于 80%；
- 很少使用二级或三级提示；
- 完成时间相对儿童自身稳定；
- 同一能力连续正确；
- 没有明显猜测。

提升顺序为减少支架、加入缺数、扩大数域、最后才开放跨十。不得越过章节和家长上限。

### 13.4 掌握证据

- 无提示独立完成：完整证据；
- 一级提示完成：部分证据；
- 二级提示完成：较弱证据；
- 三级共同完成：只计最终完成，不计独立掌握；
- 操作错误和系统中断不降低数学掌握。

---

## 14. 技术架构

### 14.1 总体结构

```text
HTML5/PWA 客户端
├── 场景与角色
├── 积木领域状态与 Reducer
├── DOM 交互代理 + Canvas 视觉层
├── Pointer 拖动与吸附
├── AudioManager
├── IndexedDB 事件 Outbox
├── 本地进度快照
└── 共享规则引擎（浏览器构建）
             │
             │ 幂等批量同步
             ▼
Node.js / Express
├── 匿名设备会话
├── 同步事件验证
├── 共享规则引擎（Node 构建）
├── SQLite 事件与权威投影
├── 课程/难度/成长服务
├── 阶段规则报告
└── AI Provider Adapter
```

### 14.2 推荐目录

```text
chick-number-blocks/
├── client/
│   ├── scenes/
│   ├── blocks/
│   ├── interaction/
│   ├── audio/
│   ├── storage/
│   └── styles/
├── server/
│   ├── routes/
│   ├── sync/
│   ├── projections/
│   ├── reports/
│   ├── ai/
│   └── database/
├── shared/
│   ├── curriculum/
│   ├── rules/
│   ├── schemas/
│   └── events/
├── content/
│   ├── chapters/
│   ├── narration/
│   └── assets/
└── tests/
```

共享规则源码同时构建为浏览器和 Node.js 版本，确保离线出题与服务端复算一致。

### 14.3 离线原则

- 当前章节内容和静态资源由 Service Worker 缓存；
- IndexedDB 保存档案、任务快照、进度快照和事件 Outbox；
- 每次学习操作先在一个事务中追加事件并更新快照，再播放永久奖励；
- 联网后按设备序号批量同步；
- MVP 只支持单档案、单设备，不处理多设备合并。

---

## 15. 同步事件与 API

### 15.1 事件信封

```json
{
  "eventId": "uuid",
  "profileId": "uuid",
  "deviceId": "uuid",
  "clientSeq": 104,
  "eventType": "TASK_COMPLETED",
  "schemaVersion": 1,
  "rulesetVersion": "1.0.0",
  "occurredAt": 1784970000000,
  "payload": {}
}
```

最小事件类型：

- `SESSION_STARTED` / `SESSION_ENDED`；
- `LEVEL_STARTED` / `LEVEL_COMPLETED`；
- `TASK_ISSUED`；
- `TASK_ATTEMPT_RECORDED`；
- `HINT_USED`；
- `TASK_COMPLETED`；
- `PARENT_SETTINGS_CHANGED`。

不记录完整触摸轨迹、原始语音和逐帧坐标。

### 15.2 API 边界

- `POST /api/v1/device-sessions`：注册匿名设备和档案；
- `GET /api/v1/bootstrap`：返回版本、快照、设置和报告摘要；
- `GET /api/v1/content/manifest`：课程、规则和资源完整性信息；
- `POST /api/v1/sync/events`：批量幂等同步；
- `GET /api/v1/sync/snapshot`：获取权威快照；
- `GET /api/v1/sync/status`：查看同步状态；
- `GET/PATCH /api/v1/parent/settings`；
- `GET /api/v1/parent/reports/latest`；
- `GET /api/v1/parent/data-export`；
- `DELETE /api/v1/parent/profile`。

不提供公开 AI 触发接口。

### 15.3 同步幂等

- `eventId` 唯一；
- `deviceId + clientSeq` 唯一；
- 序号缺口返回 `409 SYNC_SEQUENCE_GAP`；
- 重复事件返回原确认结果；
- 单个非法事件可拒绝并推进序号，不永久阻塞 Outbox；
- 奖励只在首次关卡完成投影事务中结算；
- 客户端不能上报最终奖励、掌握度和有效难度。

---

## 16. 数据模型

主要 SQLite 表：

- `profiles`：预设昵称和小鸡外观；
- `device_sessions`：匿名设备会话；
- `device_sync_state`：客户端序号和服务端 revision；
- `sync_events`：不可变同步事件和接受/拒绝状态；
- `game_sessions`：活跃游戏时间和退出原因；
- `level_runs`：章节、小关、首次完成和奖励快照；
- `task_instances`：种子、规则版本、数学规格和表现快照；
- `task_attempts`：每次确认答案、正确性和错误类别；
- `task_hints`：提示层级；
- `skill_snapshots`：技能统计和难度向量；
- `progress_snapshots`：章节、新家和奖励投影；
- `parent_settings`：家长设置及版本；
- `stage_reports`：规则报告与 AI 增强报告；
- `ai_jobs`：持久化异步任务、租约和重试。

SQLite 启用外键、WAL、5 秒 busy timeout 和迁移管理；结算、同步投影和报告触发必须使用事务。

### 16.1 错误分类

```text
MATH_INCORRECT
MATH_NEAR_NUMBER
MATH_ADD_SUB_CONFUSION
MATH_FALLEN_VS_REMAINING
MATH_MISSING_AMOUNT
MATH_CROSS_TEN
OPERATION_MISDROP
OPERATION_CANCELLED
OPERATION_RANDOM_CLICK_PATTERN
NO_RESPONSE
SYSTEM_INTERRUPTED
```

只有规则能够从题型和答案可靠推断时才写具体数学误区，否则保留通用错误，避免伪诊断。

---

## 17. 阶段报告与 AI

### 17.1 每 5 关报告

第五个不同小关首次完成时，在同一事务中：

1. 固化 5 关数据窗口；
2. 生成阶段统计；
3. 生成规则报告；
4. 计算下一章规则难度；
5. 写入唯一报告；
6. 创建唯一 AI 任务；
7. 解锁下一章。

规则报告立即可读，AI 不可用不影响解锁。

### 17.2 报告口径

- 相对稳定：同技能至少 5 个样本，首次正确率不低于 80%，二三级提示率不高于 20%；
- 需要巩固：至少 3 个样本，首次正确率低于 60% 或二三级提示率达到 40%；
- 样本不足：显示“继续观察”，不得下掌握或薄弱结论；
- “掉了多少/还剩多少”混淆必须至少出现两次受控混淆答案；
- 报告只描述行为和支架条件，不评价智力、人格或心理。

### 17.3 AI Provider

```ts
interface StageAnalysisProvider {
  analyze(
    input: StageAnalysisInput,
    options: { signal: AbortSignal }
  ): Promise<StageAnalysisRecommendation>;
}
```

提供：

- `RulesOnlyProvider`；
- `OpenAIResponsesProvider`；
- 可选 `OpenAIChatCompletionsProvider`，用于兼容服务。

模型只接收阶段聚合，不发送昵称、档案 ID、设备 ID、逐题文本、精确时间、自由输入、触摸轨迹或语音。

输出使用严格枚举 JSON Schema：进步趋势、掌握技能、待巩固技能、受控误区、难度建议、家长建议代码和置信度。MVP 不展示模型自由文本，中文建议由本地模板生成。

AI 建议必须通过证据校验、课程上限和家长设置校验。无 Key、超时、限流、Schema 错误或证据矛盾时整份作废，保留规则报告。

---

## 18. 儿童隐私与数据保留

- 只使用预设昵称；
- 不采集生日、学校、地址、联系方式、广告 ID、位置、相册和原始语音；
- 不保存完整触摸轨迹；
- 不接入广告和通用行为分析 SDK；
- 日志不记录 Cookie、答案正文、事件 payload、昵称和模型输入；
- 家长可导出和删除数据；
- 已删除档案的迟到离线事件必须被 tombstone 拒绝，不能恢复档案。

建议默认保留：

- 原始同步事件投影成功后 30 天；
- 会话明细 90 天；
- 单题学习记录 180 天；
- 阶段报告和新家进度保留到家长删除或连续 12 个月无活动。

正式上线前需按目标地区完成儿童隐私和内容安全审查。

---

## 19. 首个技术验证切片

首个切片用于验证核心机制，不作为正式 C1-L1 课程难度：

> 屋顶需要 8 块，现在有 5 块，请帮助小鸡补够。

### 19.1 切片流程

1. 展示 5 个单位积木、目标 8 和 3 个透明空位；
2. 提供 `[2]`、`[3]`、`[4]` 三个数字积木；
3. 儿童选择或拖动 `[3]`；
4. `[3]` 展开成恰好 3 个单位；
5. 三个单位依次吸附，数量牌 `5 → 6 → 7 → 8`；
6. 小鸡完成屋顶；
7. 风吹落 2 块，数量变为 6；
8. 儿童回答“还剩多少”，再补回 2 块；
9. 屋顶恢复并记录任务结果。

### 19.2 切片包含

- 预设小鸡；
- 数字、圆点、单位积木和透明空位；
- 点击与拖动两条路径；
- 自动吸附和回弹；
- 自动读题、数字读数、成功/提醒音；
- 三层提示；
- 掉落与补回；
- 动画中断恢复；
- IndexedDB 快照和 Outbox；
- 事件同步与服务端重算；
- 操作错误/数学错误区分；
- 明确的新家成长。

### 19.3 验收

1. 任意时刻实际积木数、数量牌、空位、语音和记录一致；
2. `[3]` 展开后恰好产生 3 个单位；
3. 拖偏回弹且不计数学错误；
4. 掉落 2 块时恰好播放并记录 2 块；
5. 第三层提示后儿童仍执行最后一步并获得基础奖励；
6. 刷新、切后台或动画中断后不多块、不少块；
7. 静音、低动画、竖屏和语音失败时仍能完成；
8. 服务端重复同步不会重复奖励；
9. 单题事件可以重建儿童经历和最终状态。

---

## 20. 测试设计

### 20.1 规则与性质测试

- 固定种子和版本生成相同规范 JSON；
- 遍历章节、题型、难度和大量种子，验证范围、非负减法、跨十规则和唯一答案；
- 数字牌、积木、空位、语音参数和动画 delta 一致；
- 十积木组合/展开数量守恒；
- 防重复硬规则永不违反；
- DDA 一次最多调整两个维度，不越过课程或家长上限；
- 操作失误不降低数学掌握。

### 20.2 前端测试

- Pointer 拖动、取消、吸附和回弹；
- 点击替代拖动；
- 三层提示和无操作计时；
- 语音、音效、静音和失败降级；
- 动画中断重绘；
- 横屏、竖屏、低动画和触摸目标；
- IndexedDB 原子写入和恢复。

### 20.3 同步与后端测试

- 重复事件、乱序、序号缺口和断点续传；
- 客户端投影与服务端重放一致；
- 重玩不重复推进章节或触发报告；
- 第五关并发同步只创建一个报告和一个 AI 任务；
- 已删除档案不能被迟到事件恢复；
- SQLite 事务和迁移可重建权威快照。

### 20.4 AI 测试

- 无 Key、超时、429、5xx 和非法 JSON；
- 未知枚举、越界难度和证据不足；
- 请求不含儿童身份和逐题内容；
- 任意 AI 失败下规则报告仍可读；
- worker 中断后任务可恢复。

### 20.5 儿童试玩

重点观察：

- 是否理解数字积木代表多个单位；
- 是否能区分“掉了多少”和“还剩多少”；
- 十积木是否被理解为十个一；
- 5 个任务是否过长；
- 多只小鸡和同时出现的数字/圆点/实物是否分散注意力；
- “新家”或“城堡”哪个词更能激发兴趣。

---

## 21. 多 Agent 实施边界

在实现前先冻结 `TaskSpec`、事件 Schema、数量不变量、课程配置和首个切片黄金样例。

| Agent | 单写职责 |
|---|---|
| 集成/平台 | 共享契约、依赖、构建、CI、版本和合并 |
| 规则/课程 | 数量模型、题目生成、判题、DDA 和性质测试 |
| 前端交互 | 场景、DOM/Canvas、拖动、音频、提示和离线快照 |
| 后端/数据/AI | 会话、事件同步、SQLite 投影、报告和 Provider 适配 |
| 内容/QA | 章节配置、语音键、场景包装、E2E 和儿童测试脚本 |

共享契约只能由集成负责人修改；跨模块变更先更新契约测试和黄金样例。

---

## 22. 设计完成定义

进入正式编码前必须确认：

- 数量模型和十积木不变量已冻结；
- 技术验证切片的任务 JSON 和事件序列已有黄金样例；
- 前端状态机和拖动容错可由自动测试覆盖；
- 6 章 MVP 课程表已由产品/教学人员确认；
- 预录语音、角色和建筑资产有清单及占位规范；
- 家长设置默认值和数据保留策略已确认；
- 儿童试玩招募、观察指标和停止条件已定义；
- AI 完全关闭时仍有完整游戏和报告路径。

本设计说明冻结后，下一步应先实现第 19 节的技术验证切片，再扩展为 C1 正式课程和完整 30 关。
