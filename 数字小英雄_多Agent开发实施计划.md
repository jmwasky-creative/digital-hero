# 《数字小英雄》多 Agent 开发实施计划

## 1. 总体目标与第一性原则

以 4 Agent 并行开发，在约 12 个工作日内交付可部署、可试玩、可持续扩展的 Web MVP。

MVP 验收闭环：

> 匿名创建英雄 → 进入桃源村 → 完成 5 个小关卡 → 每关 5 道题 → 动态调整难度 → 结算经验/金币 → 生成家长学习报告 → 刷新后进度仍保留。

所有设计从以下不变量推导：

- 数学正确性优先：出题、判题、奖励、技能解锁和实时难度全部由确定性规则负责。
- 儿童不会被卡住：不设倒计时和战斗失败；错误后提供提示，两次错误后引导完成。
- 服务端是唯一事实源：客户端不能决定答案、奖励、等级或关卡完成状态。
- AI 是可拔掉的增强层：只分析每 5 关的聚合数据，失败不能阻塞游戏。
- 数据最小化：不收集真实姓名、生日、学校、地址、照片、录音等儿童个人信息。
- 契约先于并行：OpenAPI、数据库迁移、JSON Schema、状态机和黄金测试样例冻结后，工作 Agent 才开始编码。

## 2. MVP 产品与领域规则

### 游戏及学习闭环

- MVP 完整实现桃源村 5 个小关卡，其他 4 张地图展示为未解锁，留待内容扩展阶段。
- 每关是一场 5 题战斗，建议时长 3–5 分钟；怪物有 5 点能量，每解决一题减少 1 点。
- 每题流程固定为：题目播报/图示 → 3 个大按钮 → 判题 → 动画反馈 → 下一题。
- 第一次答错：不扣奖励，展示十格框、实物或数轴提示后重试。
- 第二次仍错：展示分步演示并结束本题，标记为“辅助完成”，不计为掌握。
- 星级按首次无辅助答对数结算：5 题为 3 星，4 题为 2 星，0–3 题为 1 星。
- 每题答对奖励 `10 EXP + 5 金币`，通关额外奖励 `20 EXP + 10 金币`；升级所需经验为 `100 × 当前等级`。
- 金币先持久化但不开放商店；完整宠物、碎片、装扮系统放到 MVP 指标通过后的扩展阶段。

### 技能及题目规则

- `story_math` 从独立技能改为 `presentationType`，避免把题目表达形式与数学能力混为一谈。
- `skillName` 保留 7 类能力：数字基础、10 内加法、20 内不进位加法、20 内进位加法、10 内减法、20 内不退位减法、20 内退位减法。
- `presentationType` 为 `symbolic | visual | story`。
- 所有操作数及结果必须是 `0..20` 整数；减法不得出现负数。
- 每题 3 个互异选项，必须且只能包含一个正确答案；同一关不重复题目。
- 题库由本地生成器和审核过的故事模板提供，MVP 不让 AI 生成儿童题目。
- 桃源村默认开放数字基础、10 内加减；题目引擎和测试覆盖全部技能，高阶技能由后续地图课程配置解锁。

### 动态难度

- 难度固定为 `1..3`，初始为 1；每关结束最多升降一级。
- 按当前技能最近最多 20 次首次作答计算；不足 10 次时保持当前难度。
- 正确率 `>85%`、提示率 `≤20%` 且中位答题时间不超过题目期望时间的 `1.25` 倍：升一级。
- 正确率 `<70%`、提示率 `>40%` 或连续答错 3 题：降一级。
- 正确率位于 `70%..85%`、数据不足或只是答题较慢：保持。
- AI 只能把已解锁技能权重调整到 `0.8..1.2`，不能解锁技能或直接修改难度。
- `masteryScore` 使用最近 20 题的平滑证据分计算；少于 5 题时前端显示“数据积累中”，不展示伪精确分数。

## 3. 技术架构、接口与数据

### 工程结构

继续采用设计文档指定技术栈：HTML/CSS/JavaScript、DOM + Canvas、Node.js、Express、SQLite、OpenAI JavaScript SDK。

- DOM 承担题目、按钮、提示、报告等可交互内容。
- Canvas 只承担角色、怪物和技能特效，保证触控、测试和可访问性。
- 后端采用模块化单体：`routes → application services → pure domain → repositories`。
- 领域层不依赖 Express、SQLite 或 OpenAI，便于独立测试与多 Agent 并行。
- OpenAI、语音和题库都通过适配器封装，可使用 fake 实现完成离线测试。

### REST API v1

统一使用 `/api/v1`，错误格式为 `{ error: { code, message, retryable, requestId } }`。

- `POST /players`：使用预设昵称、头像和 `clientRequestId` 创建匿名角色。
- `GET /me`、`GET /me/progress`：读取玩家和地图进度。
- `DELETE /me`：级联删除玩家全部学习数据。
- `POST /runs`：幂等创建一场小关卡。
- `POST /runs/:runId/questions/next`：发放或恢复当前未完成题目，响应绝不包含答案。
- `POST /attempts/:attemptId/hint`：幂等记录提示使用。
- `POST /attempts/:attemptId/answers`：支持最多两次作答；每次选择带唯一 `responseId`，网络重试返回原结果。
- `POST /runs/:runId/finish`：服务端检查 5 题完成状态，在单一事务中结算奖励、升级和报告检查点。
- `GET /reports/latest`、`GET /reports/:reportId`：读取规则或 AI 增强报告。
- `/health/live`、`/health/ready`：检查进程、SQLite 可写性和迁移版本；OpenAI 不可用不影响 readiness。
- 取消公开 `POST /api/ai/analyze`，避免客户端制造 AI 成本；报告由第 5 关结算事件自动触发。

### SQLite 模型

在原设计文档基础上补齐关系和幂等约束：

- `players`：匿名角色、头像、等级、经验、金币、完成关数。
- `player_sessions`：哈希会话令牌和有效期；使用 HttpOnly、SameSite Cookie。
- `player_skills`：技能、掌握分、当前难度、样本数、AI 权重。
- `questions`：技能、难度、题面、选项、答案、提示、内容版本。
- `game_runs`：地图、关卡、状态、题目数量、奖励快照和幂等请求 ID。
- `question_attempts`：已发题目、首次结果、尝试次数、提示和辅助完成状态。
- `answer_events`：每次点选的不可变事件及 `responseId`。
- `ai_jobs`：检查点、状态、重试时间和唯一约束。
- `ai_reports`：规则报告、AI 增强结果、模型、Prompt 版本和输入哈希。
- `schema_migrations`：数据库版本。

SQLite 启用外键、WAL、超时和参数化查询；结算使用事务与唯一约束，保证重复提交、双击或并发请求不会重复发奖。

### AI 阶段分析

- 第 5 个已结算关卡先同步生成规则报告，再创建持久化 AI 任务。
- 只发送技能级题数、正确率、提示率、中位时间、掌握度变化等聚合值；不发送昵称、玩家 ID、逐题文本或设备信息。
- 使用 Responses API 和严格 JSON Schema，输出只允许受控枚举：趋势、薄弱技能、原因代码、权重倍率、家长建议代码。
- 中文建议由本地模板渲染，不直接展示自由生成文本。
- API Key 仅存服务端环境变量，禁止进入浏览器代码。
- `AI_ANALYSIS_ENABLED` 控制真实调用；无密钥、超时、限流或非法输出时保留规则报告。
- 模型通过 `OPENAI_MODEL` 配置，保存模型和 Prompt 版本；AI 请求使用 `store:false`。
- 正式儿童上线前必须完成适用地区的隐私与儿童保护审查。

## 4. 四 Agent 并行实施

### 角色与代码所有权

| Agent | 主要职责 | 单写目录/文件 |
|---|---|---|
| A0 集成与平台 | 契约、依赖、迁移、会话、路由编排、CI、合并与发布 | `server.js`、依赖清单、`contracts/`、`src/database/`、公共配置 |
| A1 学习领域 | 题目生成、选题、判题、掌握度、DDA、奖励和课程配置 | `src/domain/`、`data/questions/`、对应单元测试 |
| A2 前端游戏 | 建角、地图、战斗状态机、Canvas 动画、DOM 交互、语音和恢复 | `public/`、前端组件测试 |
| A3 AI 与质量 | 聚合、规则报告、AI 适配器、任务重试、隐私测试和 E2E | `src/ai/`、`tests/e2e/` |

共享契约、依赖锁文件和公共 fixture 只允许 A0 修改。其他 Agent 如需调整契约，提交包含动机、兼容性和测试影响的变更请求。

### 工作波次

| 波次 | 时间 | 并行工作与出口条件 |
|---|---:|---|
| W0 契约冻结 | 第 1 天 | A0 建立 Git、目录、OpenAPI、JSON Schema、迁移 v1、Mock API、状态机和黄金样例；全员评审后冻结 |
| W1 独立实现 | 第 2–5 天 | A0 做数据库和 API 骨架；A1 做纯领域引擎；A2 基于 Mock 完成前端；A3 完成规则报告和 Fake AI |
| W2 纵向集成 | 第 6–8 天 | 打通建角→5 题→结算→刷新恢复；并行接入 DDA、奖励、真实 API、AI 任务和报告页 |
| W3 内容与容错 | 第 9–10 天 | 完成桃源村 5 关、全部错误/提示流程、断网恢复、无 AI Key 降级、隐私和可访问性 |
| W4 发布硬化 | 第 11–12 天 | 全量测试、移动视口、性能、儿童/家长人工走查、修复和候选版本验收 |

依赖关系：

```text
契约冻结
   ├── 数据/API
   ├── 学习领域
   ├── Mock 前端
   └── AI/规则报告
          ↓
无 AI 纵向切片
          ↓
DDA + AI + 家长报告
          ↓
发布硬化
```

### 协作约束

- 使用 `integration/mvp-v1` 集成分支和独立 worktree：`agent/platform`、`agent/learning`、`agent/frontend`、`agent/ai-report`。
- 各 Agent 只修改自己的目录，并为自己的模块编写测试。
- 提交信息带工作包编号；A0 是唯一合并人。
- 集成顺序固定为契约 → 平台 → 领域 → AI → 前端，每次合并后跑全套门禁。
- 禁止在功能分支顺手修改共享契约或进行跨模块重构。
- 先用 Fake AI 完成全链路；全部 E2E 通过后才启用真实 OpenAI 特性开关。

## 5. 测试、验收与发布门禁

### 自动化测试

- 65% 单元/性质测试：每个技能和难度生成至少 1000 个固定种子样例，验证范围、非负减法、唯一选项、正确答案、去重和故事语义。
- 25% API/SQLite 集成测试：迁移、外键、事务、会话隔离、重复发题、重复作答、并发结算、刷新恢复和级联删除。
- 10% Playwright E2E：首次建角、完整关卡、两次答错、提示、结算、连续 5 关、规则报告、AI 成功/失败、移动端触控。
- 领域与 AI 模块分支覆盖率不低于 90%，整体行覆盖率不低于 80%。
- 数学正确性、答案不泄露、数据隐私、结算幂等和 AI 降级测试不可豁免。

### MVP 验收

- 无需输入真实信息即可创建英雄并完成首场战斗。
- 答错不会产生失败或无限循环；两次错误后可以继续游戏。
- 每题正确记录技能、难度、首次结果、答题时间、提示和辅助完成状态。
- 双击、超时重试、刷新及并发调用不会重复记录或发奖。
- 动态难度严格按冻结阈值运行，且一次只升降一级。
- 第 5 关后一定生成基础报告；OpenAI 关闭或失败时游戏和报告仍可用。
- 前端点击到本地反馈小于 100ms，非 AI API 本机 P95 小于 200ms，首屏目标小于 3 秒。
- 常见桌面与平板视口不遮挡；触控目标至少 64 CSS px；音频失效时仍有文字和图示。
- 观察性试用中，至少 85% 的目标儿童无需成人操作帮助即可进入并完成首场战斗，已开始战斗完成率目标不低于 80%。
- 学习效果另以等价前后测验证，不用游戏内正确率直接宣称教学成效。

## 6. 默认范围与后续阶段

本计划默认采用“可部署试点版 + 可替换原创占位资源 + 真实 OpenAI 接入并自动降级”。

MVP 不包含账号/跨设备同步、正式商店、完整宠物和装扮、微信小游戏、AI 对话助手、正式美术与真人配音，也不代表已经完成任何地区的正式儿童产品合规认证。

MVP 指标通过后，按数据驱动配置依次开放迷雾森林、云海山、幽暗洞穴和龙之谷，并增加正式宠物、装扮、授权资源及完整家长控制；领域接口和数据库无需破坏性重写。
